#pragma once

enum class ResourceType {
	Uninitialized = 0,  // Represents an uninitialized resource type
	IMAGE,  // Represents an image resource
	SOUND,  // Represents a sound resource
	FONT    // Represents a font resource
};
